specVersion: "v1beta"
serviceId: "login-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "login-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
