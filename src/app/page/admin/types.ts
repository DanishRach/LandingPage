import { ReactNode } from "react";

export interface TableRow {
    tagihan: ReactNode;
    createdDateTime: ReactNode;
    userID: ReactNode;
    layananID: ReactNode;
    domain: ReactNode;
    status: string;
    email: string;
    amount: number;
  }
  